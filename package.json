{
  "name": "animated-fiesta",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Animated Fiesta — a first-person confetti-cannon party game vs. The Grey Auditor.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@types/three": "^0.171.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
